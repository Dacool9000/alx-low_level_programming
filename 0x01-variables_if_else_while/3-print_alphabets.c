#include <stdio.h>
/**
 * main - retry
 * Return: 0 (success)
 */
int main(void)
{
	char c;
	char d;

	while (c <= 'z')
	{
		putchar(c);
		c++;
	)
	while (b <= 'Z')
	{
		putchar(d);
		d++;
	}
			putchar('\n');
	return (0);
}
